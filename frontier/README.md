# frontier/ — the moving-target layer

Versioned online laboratories involving current commercial and open-source agent
platforms, including coding-agent teams. This layer carries the burden of
currency, so that a reader who meets the fifth renamed version of an API is not
thereby obliged to purchase a new theory of cooperation.

Because these platforms rename their APIs at leisure, the laboratories here are
maintained separately from the conceptual core and are **dated**: each lab records
the platform versions it was last run against. Expect them to need updating; that
is the nature of the frontier, and the reason the durable content lives in
`foundations/` and the book, not here.

## Planned contents

| From | What it builds |
|:-----|:---------------|
| Ch 19 | The framework lab: one task — the running team's pipeline — built three times (a graph runtime, a vendor agent SDK, a declarative crew system) and scored on tokens, latency, lines of code, and failure behaviour. |
| Ch 21–22 | Architectural patterns and interoperability against live platforms: routers, supervisors, hand-offs; an Agent Card and an MCP server for the team's tools. |
| Ch 27 | The capstone **scaling lab**: the same task across team sizes and topologies, measuring quality, tokens, latency, and the failure-mode census — the book's central claim put to its own standard of evidence. |

## Why it is not here yet

These labs require live accounts, keys, and current vendor SDKs; they cannot be
run hermetically and they date quickly. They are the last layer to be built, and
each will ship with a version-dated note and its own setup instructions.

See **Appendix C** of the book for keys, provider setup, and how to run each lab.
